type: "library"

tasks:
  build:
    command:
      - "packemon"
      - "pack"
      - "--addEngines"
      - "--addExports"
      - "--declaration"
    inputs:
      - "@globs(sources)"
      - "package.json"
      - "tsconfig.*.json"
